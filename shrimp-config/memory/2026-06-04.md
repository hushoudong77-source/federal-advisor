# 2026-06-04

## Vibe-Trading 安装 & 配置
- 安装 `vibe-trading-ai` v0.1.9（pip3, Python 3.14）
- MCP 服务器 `vibe-trading-mcp` 配入 OpenClaw `mcp.servers`，暴露 36 个工具
- 三个入口：CLI / MCP / Web UI (port 8000)
- `TUSHARE_TOKEN` 已配入 gateway 环境变量
- 网关已重启，等待继续

## 待办
- [ ] 跑全池21标因子分析（复用 continuationMessage）
